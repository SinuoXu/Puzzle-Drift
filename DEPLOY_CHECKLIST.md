# 你现在只需要做的 4 件事

1. Supabase SQL Editor 运行 `supabase/migration_v0_2.sql`
2. Vercel 保留 `SUPABASE_URL`、`SUPABASE_SECRET_KEY`
3. 推荐新增 `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
4. 用本目录代码覆盖 GitHub 仓库并 `git push origin main`

如果部署后报错，优先把 Vercel Deployment Logs 最后 30 行发给 ChatGPT。
